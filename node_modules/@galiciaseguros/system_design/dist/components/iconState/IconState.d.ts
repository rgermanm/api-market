import { HTMLAttributes } from "react";
export interface Props extends HTMLAttributes<HTMLButtonElement> {
    label: string;
    state: 'danger' | 'success' | 'warning' | 'disabled' | 'info';
}
export declare const IconState: ({ label, state }: Props) => JSX.Element;
