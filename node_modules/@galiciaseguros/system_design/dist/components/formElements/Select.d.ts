import { HTMLAttributes } from "react";
export interface Props extends HTMLAttributes<HTMLInputElement> {
    sizeVariant: 'regular' | 'large';
    label: string;
    border?: 'bottom' | 'round' | 'shadow';
    onChange?(e?: any): any;
    isValid?: boolean;
    error?: string;
    helperText?: string;
    placeHolder?: string;
    prefix?: string;
    sufix?: string;
    format?: (val: any) => any;
    disabled?: boolean;
    register?: any;
    name?: string;
    controller?: any;
}
export declare const Select: ({ controller, name, register, format, disabled, border, sufix, prefix, placeHolder, helperText, sizeVariant, isValid, error, label, onChange, ...props }: Props) => JSX.Element;
