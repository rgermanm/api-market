import { HTMLAttributes } from "react";
interface Props extends HTMLAttributes<HTMLTextAreaElement> {
    sizeVariant: 'regular' | 'large';
    border?: 'bottom' | 'round' | 'shadow';
    hint?: string;
    placeHolder?: string;
    hintPosition?: 'left' | 'right';
    format?: (val: any) => any;
    disabled?: boolean;
}
export declare const TextArea: ({ format, disabled, border, placeHolder, hint, hintPosition, sizeVariant, ...props }: Props) => JSX.Element;
export {};
