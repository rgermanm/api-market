import { HTMLAttributes } from "react";
export interface Props extends HTMLAttributes<HTMLInputElement> {
    disabled?: boolean;
    disabledPosition?: "left" | "right";
    desription?: string;
    textOn?: string;
    textOff?: string;
    initialValue?: boolean;
}
export declare const Switcher: ({ initialValue, disabled, textOn, textOff, desription }: Props) => JSX.Element;
