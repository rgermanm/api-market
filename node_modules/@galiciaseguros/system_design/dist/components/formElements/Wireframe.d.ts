import { HTMLAttributes, ReactNode } from "react";
interface Props extends HTMLAttributes<HTMLDivElement> {
    children: ReactNode;
}
export declare const Wireframe: ({ children, ...props }: Props) => JSX.Element;
export {};
