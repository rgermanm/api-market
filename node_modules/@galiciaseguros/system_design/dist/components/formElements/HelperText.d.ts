import { HTMLAttributes } from "react";
interface Props extends HTMLAttributes<HTMLParagraphElement> {
    children: string;
    variant?: "info" | "success" | "error";
}
export declare const HelperText: ({ variant, children, ...props }: Props) => JSX.Element;
export {};
