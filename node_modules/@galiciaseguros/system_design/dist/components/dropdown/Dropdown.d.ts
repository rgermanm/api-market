import { HTMLAttributes } from "react";
interface Props extends HTMLAttributes<HTMLDivElement> {
    options: any;
    setSeleted: (e?: any) => any;
    disabled?: boolean;
    open?: boolean;
}
export declare const Dropdown: ({ options, open, setSeleted }: Props) => JSX.Element;
export {};
