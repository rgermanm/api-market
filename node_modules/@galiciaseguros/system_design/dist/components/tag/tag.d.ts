import { HTMLAttributes } from "react";
export interface Props extends HTMLAttributes<HTMLButtonElement> {
    label: string;
    error: boolean;
    isVisible: boolean;
}
export declare const Tag: ({ label, error, isVisible }: Props) => JSX.Element;
