import { HTMLAttributes } from "react";
export interface Props extends HTMLAttributes<HTMLButtonElement> {
    canvas?: "A4" | "A5";
    title?: string;
    origin?: "OFB" | "ONB";
    tipoOperacion: string;
    details: Array<Detail>;
    conceptos: Array<Detail>;
    legales: string;
}
interface Detail {
    title: string;
    description: string;
}
export declare const Comprobante: ({ legales, conceptos, details, canvas, title, origin, tipoOperacion }: Props) => JSX.Element;
export {};
