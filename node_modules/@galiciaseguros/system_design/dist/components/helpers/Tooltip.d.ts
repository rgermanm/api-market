import { HTMLAttributes } from "react";
interface Props extends HTMLAttributes<HTMLButtonElement> {
    text: string;
    variant: 'light' | 'dark';
    disabled?: boolean;
}
export declare const Tooltip: ({ text, variant }: Props) => JSX.Element;
export {};
