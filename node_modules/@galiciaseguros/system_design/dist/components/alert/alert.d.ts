import { HTMLAttributes } from "react";
export interface Props extends HTMLAttributes<HTMLButtonElement> {
    title: string;
    description: string;
    actionLabel?: string;
    action?(e?: any): any;
    type: "success" | "info" | "warning" | "brand" | "error";
    isVisible?: boolean;
    alertPages?: AlertPage[];
}
interface AlertPage {
    title: string;
    description: string;
    actionLabel?: string;
    action?(e?: any): any;
}
export declare const Alert: ({ title, description, actionLabel, action, type, isVisible, alertPages }: Props) => JSX.Element;
export {};
